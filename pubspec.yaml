name: mimasu
description: "An Android video app that plays what you bring to it."
publish_to: 'none'

version: 0.1.0+1

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.7.0
  flutter_bloc: ^9.0.0
  get_it: ^8.0.3
  hive_ce: ^2.10.1
  logger: ^2.5.0
  path_provider: ^2.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  pigeon: ^22.7.0

flutter:
  uses-material-design: true
